import Layout from "@components/layout";

const NotFoundPage = () => {
    return (
        <Layout>
            <h1>Oopsie whoopsie, we can't find this page</h1>
        </Layout>
    );
};

export default NotFoundPage;
